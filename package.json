{
  "name": "org-editor",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://Otakumesi.github.io/Org-Editor",
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "draft-js": "^0.9.1",
    "org": "^0.2.0",
    "react": "^15.4.1",
    "react-bootstrap": "^0.30.7",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "provide": "npm run build && rm -r docs/ && cp -r build/ docs/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
